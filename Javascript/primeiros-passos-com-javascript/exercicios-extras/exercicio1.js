const { gets, print } = require('./funcoes-auxiliares1');

madia = gets;

if (media < 5) {
    print("Repovado");
} else if (media >= 7 && media < 7) {
    print("Recuperação");
} else {
    print("Aprovado")
}