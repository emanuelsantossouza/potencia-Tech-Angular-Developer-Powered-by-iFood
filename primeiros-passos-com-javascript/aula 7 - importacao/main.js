// ---- Codigos Equivante -----
// const { nome } = pessoa;

// const nome = pessoa.nome



const { gets, print } = require("./funcoesAuxiliares");

print(gets());

const pessoa = {
  nome: "vitor",
};
